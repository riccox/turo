// first one is default lang
const SUPPORTED_LANGUAGES = ['en', 'zh'];
// first one is default ns
const NAMESPACES = ['common', 'home'];

module.exports = {
  SUPPORTED_LANGUAGES,
  NAMESPACES,
  debug: false,
  defaultNS: NAMESPACES[0],
};
